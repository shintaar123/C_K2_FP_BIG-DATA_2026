# llm_client.py & llm_enrichment.py hanya butuh stdlib (urllib, json) +
# PySpark yang sudah tersedia di image spark. Tidak ada dependency eksternal wajib.
#
# Opsional, jika ingin memakai SDK resmi (bukan REST stdlib) saat pengembangan lokal:
# google-generativeai>=0.8.0   # Gemini SDK
# groq>=0.11.0                 # Groq SDK
# cerebras-cloud-sdk>=1.0.0    # Cerebras SDK
